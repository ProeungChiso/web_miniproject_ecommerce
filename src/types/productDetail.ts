export type productDetailType = {
    id ?: number
    name ?: string,
    price ?: string | number,
    image ?: string,
    desc ?: string,
    category ?: string,
    seller ?: string,
    quantity ?: number | string,
    created_at ?: string,
    updated_at ?: string,
}